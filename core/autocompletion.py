"""zipcode_file = self.config.get( 'Autocompletion', 'zipcode_file' )
        if os.path.exists( zipcode_file ):
            self.manager.loadZipcodes( zipcode_file )
        bankcode_file = self.config.get( 'Autocompletion', 'bankcode_file' )
        if os.path.exists( bankcode_file ):
            self.manager.loadBankcodes( bankcode_file )"""